#!/usr/bin/env bash
# =============================================================================
# generate-mockups.sh — Simulator mockup generator (REMOVED)
#
# The simulator feature has been removed. This script is preserved only as a
# reference and will exit with an error if called directly.
# =============================================================================
echo "ERROR: The simulator feature has been removed. generate-mockups.sh is no longer applicable." >&2
exit 1
